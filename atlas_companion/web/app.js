const $=s=>document.querySelector(s),message=$('#message'),tasks=$('#tasks'),detail=$('#detail');async function api(url,options={}){const r=await fetch(url,{headers:{'Content-Type':'application/json'},...options});const data=await r.json();if(!r.ok)throw Error(data.error||r.statusText);return data}function esc(s){const d=document.createElement('div');d.textContent=s;return d.innerHTML}async function list(){const data=await api('/api/tasks');tasks.innerHTML=data.length?data.slice(0,30).map(t=>`<button class="task" data-id="${t.id}"><span class="status ${t.status}">${esc(t.status)}</span> ${esc(t.objective)}<br><small>${esc(t.id)}</small></button>`).join(''):'No tasks yet.';document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>show(b.dataset.id))}async function show(id){const d=await api('/api/tasks/'+id),p=d.presentation;detail.innerHTML=`<h2>${esc(p.objective)}</h2><p><span class="status ${p.status}">${esc(p.status)}</span> · ${esc(p.task_id)}</p><button id="run">Run / resume</button><button id="cancel">Cancel</button><h3>Result</h3><pre>${esc(d.markdown)}</pre>${p.pending_approvals.map(a=>`<article><p>${esc(a.requested_action)}</p><button data-approval="${a.id}" data-action="approve">Approve</button><button data-approval="${a.id}" data-action="deny">Deny</button></article>`).join('')}`;$('#run').onclick=()=>act('/api/tasks/'+id+'/run');$('#cancel').onclick=()=>act('/api/tasks/'+id+'/cancel');document.querySelectorAll('[data-approval]').forEach(b=>b.onclick=()=>act('/api/approvals/'+b.dataset.approval+'/'+b.dataset.action))}async function act(url){try{const d=await api(url,{method:'POST',body:'{}'});message.textContent='Updated.';await show(d.presentation.task_id);await list()}catch(e){message.textContent=e.message}}$('#new-task').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{const d=await api('/api/tasks',{method:'POST',body:JSON.stringify({objective:f.get('objective'),criteria:f.get('criteria')})});message.textContent='Task created.';e.target.reset();await show(d.presentation.task_id);await list()}catch(err){message.textContent=err.message}};list().catch(e=>message.textContent=e.message);
